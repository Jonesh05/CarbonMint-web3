import type { NextPage } from "next";
import React from "react";

const ProviderPage: NextPage = () => {
    return (
     <div>
        <h1>Provider Page</h1>
        <p>Esta es una página nueva creada para la ruta /provider.</p>
    </div>
    );
};

export default ProviderPage;